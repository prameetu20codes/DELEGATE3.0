apiVersion: v1
kind: Service
metadata:
  name: {{.Values.name}}-svc
  labels:
    app: {{.Values.name}}
spec:
  type: {{.Values.serviceType}}
  selector:
    app: {{.Values.name}}
  ports:
  - port: {{.Values.servicePort}}
    targetPort: {{.Values.servicePort}}
    protocol: TCP
